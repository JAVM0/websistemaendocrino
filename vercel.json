{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/css/(.*)", "dest": "/style.css" },
    { "src": "/js/(.*)", "dest": "/main.js" },
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
